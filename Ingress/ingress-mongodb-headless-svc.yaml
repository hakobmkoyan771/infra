apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-mongodb-headless-svc
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - pathType: Exact
            path: /
            backend:
              service:
                name: mongodb-headless
                number: 27017
